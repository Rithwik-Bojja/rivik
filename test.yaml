schedules:
  - cron: "30 4 * * 3"  # 4:30 AM UTC every Wednesday
    displayName: "10:00 AM IST - Wednesday run"
    branches:
      include:
        - master
    always: true  # Ensure it runs even if no new changes
